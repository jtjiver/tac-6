# (Optional) Anthropic API Key - only needed if you DON'T have Claude Code Pro
# With Claude Code Pro subscription, Claude will use your authenticated session automatically
# Only set this if you want to use a specific API key or don't have Pro subscription
# WARNING: Setting this will consume API credits and cost money!
ANTHROPIC_API_KEY=

# (Optional) ADW API Usage Control
# Set to 'false' to disable automated ADW workflows (default: 'true')
# Only set this to 'false' if you want to block Claude Code execution in ADW scripts
# ADW_ALLOW_API_USAGE=true

# (Optional) Git Signing Control for Automated Workflows
# If you use 1Password or other interactive GPG signing for git commits,
# set this to 'true' to disable signing in automated ADW workflows (default: 'true')
# This prevents authentication prompts that would block autonomous agents
# Your interactive terminal sessions will still use GPG signing as configured
# ADW_GIT_DISABLE_SIGNING=true
#
# Advanced: Override git config parameters directly (for experts)
# ADW_GIT_CONFIG_PARAMETERS="'commit.gpgsign=false' 'user.name=ADW Bot'"

# GitHub Configuration (Optional)
# Use this if you want ADW to use a different GitHub account than your default 'gh auth' login
# If not set, ADW will use whatever GitHub account is authenticated with 'gh auth login'
# (Optional)
GITHUB_PAT=

# (Optional) Claude Code Path - if 'claude' does not work run 'which claude' and paste that value here
CLAUDE_CODE_PATH=claude

# (Optional) Agent Cloud Sandbox Environment
E2B_API_KEY=

# (Optional) Helicone API Key for monitoring LLM API calls
# Get your key at https://helicone.ai
HELICONE_API_KEY=

# (Optional)( Returns claude code to the root directory after every command
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true

# (Optional If you use cloudflare's tunnel to expose `adws/trigger_webhook.py` to github (public internet)
CLOUDFLARED_TUNNEL_TOKEN=

# (Optional) Cloudflare R2 Configuration for uploading review screenshots
# If configured, review screenshots will be uploaded to R2 and displayed as images in GitHub comments
# If not configured, local file paths will be shown instead
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET_NAME=
# Public domain can be with or without https:// prefix
# Examples: "pub-xxx.r2.dev" or "https://pub-xxx.r2.dev" or "custom-domain.com"
CLOUDFLARE_R2_PUBLIC_DOMAIN=
